{
  "name": "badminton-scheduler-worker",
  "version": "1.12.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:courtly": "npx wrangler deploy --config wrangler.courtly.toml",
    "check": "node --check src/index.js"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
