# Handoff - 2026-07-08

## What changed today

- Added live shared schedule sync so viewers on the same `scheduleCode` re-fetch the canonical schedule when it changes.
- Kept shared viewers on their current round instead of jumping them to the end after another user extends the schedule.
- Made shared `+5 rounds` idempotent by sending the current round count as the shared extend base and returning the canonical schedule from the server.
- Added a live update endpoint on the Node server and Durable Object fanout on the Worker backend.
- Fixed the Cloudflare Worker deployment failure caused by the Durable Objects migration format on the free plan.
- Triggered Cloudflare deployment twice from `main`, including a no-op commit to force a fresh deploy.

## Current state

- Branch: `main`
- Remote: `origin/main`
- Latest commits today:
  - `4ec93a1` `fix: use sqlite durable object migration`
  - `d71513f` `chore: trigger cloudflare deploy`
  - `72eb61a` `feat: preserve viewer round on shared sync`
- Unrelated local noise such as `.claude/` was left untouched.

## Verification

- `node -c server.js`
- `node --check worker/src/index.js`
- Local smoke test confirmed shared extend stays canonical and stale extend requests do not fork the schedule.
- Cloudflare build failure was reproduced, then fixed by switching the Worker migration to `new_sqlite_classes`.

## Next steps

- Confirm the Cloudflare production deploy completes from `main`.
- Do a browser check with two clients on the same shared QR link and verify:
  - one user extending adds rounds for everyone,
  - everyone stays on their current round index,
  - the round label updates to show the new total.
