# CLAUDE.md

This file stays intentionally small.

Read [AGENTS.md](AGENTS.md) first and treat it as the primary repo guidance for this workspace.

Run `npm test` for any change to the scheduling algorithm — it is the regression suite for rotation fairness and it checks all three copies of the algorithm stay aligned. Use the codebase and manual verification for UI behavior that needs live confirmation.
